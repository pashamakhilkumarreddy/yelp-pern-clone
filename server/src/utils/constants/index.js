const database = {
  DB_RESEARCH: "research",
};

const constants = {
  DB: {
    ...database,
  },
  DB_TABLES: {
    TBL_USERS: `users`,
    TBL_RESTAURANTS: `restaurants`,
  },
};

export default constants;
